{
  "name": "live-noti-fire",
  "version": "1.0.0",
  "description": "Cloudflare Worker with Hono",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy src/index.ts"
  },
  "dependencies": {
    "hono": "^3.12.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
